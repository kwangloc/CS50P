import cowsay
import sys 

if len(sys.argv) == 2:
    # statement = "hello, i'm " + sys.argv[1] + ", im a silly but kinda cute cow     and red flags are my favourite"
    # cowsay.cow(statement)
    cowsay.trex("hello, i'm " + sys.argv[1])